- Create network:
    docker network create leaguer-network

- Create postgres container:
    docker run --name postgres-container --network=leaguer-network -e POSTGRES_USER=db_user -e POSTGRES_PASSWORD=db_password -e POSTGRES_DB=leaguer -p 5432:5432 -d postgres
